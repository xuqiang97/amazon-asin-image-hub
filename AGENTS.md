# 项目协作规则

本文件供在不同电脑、不同 Codex 任务中接续 `amazon-asin-image-hub` 使用。用户在当前会话中的明确指示优先。

## 开始工作

1. 查看 Git 分支、工作区改动和远端关系，保护尚未提交的用户工作。用户要求跨电脑接续时，先获取远端状态；工作区干净且可快进时再拉取，遇到分叉先检查差异。
2. 完整阅读本文件和 `README.md`，按需查看 `index.html`。
3. 需要追溯背景时读两份原始交接文档；它们是历史资料，旧结论不得覆盖最新用户确认和 README。
4. 不假设能访问上一台电脑的对话、长期记忆、绝对目录、凭据、插件或本机服务。

业务结论以最新用户确认和 README 为准；原型实现以代码和实际检查为准。发现冲突时明确指出，不默默把演示行为升级为业务规则。

## 协作方式与授权

- 使用中文，表达轻量、直观，一次推进一个主要问题。先说明对用户操作有什么影响，再补必要技术细节。
- 明确区分“用户已确认”“设计建议”“研发待核实”“原型已演示”，不要把建议或原交接中的接口说法当成验证结果。
- “先检查／先讨论／先不要修改”意味着只读检查和讨论，等明确授权再制作或修改文档、原型、代码。
- 本地实施与 GitHub 发布分别判断授权。用户明确要求“发布到 GitHub”“把进度推到仓库”等即授权当次范围内的整理、提交、推送及既有 Pages 部署，无须重复询问；不将某次授权扩展成未来无限发布权限。
- 2026-09-14 已授权整理当前全部项目资料并发布 GitHub，供两台电脑轮流拉取、更新、推送。本记录不替代后续新任务的范围判断。
- 用户希望独立思考和专业建议，不让业务替研发决定接口实现；未确定的规则分批讨论，不一次列大量问题要求全部回答。

## 当前产品边界

- 商品图与 A+ 图分为独立 Tab；先细化 A+，商品图仍在整体范围内。
- 首版聚焦新产生的推送业务。历史素材可用于新组稿；存量迁移、历史整改、批量补标重推仅预留，不自行加入本期。
- A+ 列表一行一条独立推送记录；店铺＋ASIN 为素材归属维度。生图任务与推送任务 ID 分开。
- A+ 仅提供有序图片，不在业务页面添加模板或文字字段。本地上传检测通过后本人可确认推送。
- 保留单个完整任务审核通过后的自动推送方向；不自动跨任务凑图。“7 张”是原交接的已有场景，通用数量限制未定。
- 权限、真实检测链路、接口状态、幂等、并发和重试等以 README 中的状态为准，不擅自补成定案。
- 商品图 MSKU 多候选问题已搁置；FBA／FBM 并存是用户确认的常见场景，PanEU／二手只是待问线索。不要阻塞当前 A+ 讨论。
- 我方页面完成业务操作并保存来源、版本、使用位置、快照和结果；下游负责平台转发与回传。前端演示不等于真实服务已接通。

## 文件与原型约定

- `README.md` 是唯一持续维护的需求与接续说明，当前为讨论稿。先在这里沉淀确认项、待办和进度，不额外创建重复 PRD、状态文档或交接总结。
- `index.html` 是唯一页面原型。简单骨架直接写成完整、自包含 HTML，用户用 Chrome 打开即可。
- 不为对话内展示制作额外 HTML 片段、包装页或预览页；不把页面画成图片。不引入仅在本机或 Codex 对话中可用的运行器。
- 保留现有交互，除非用户确认调整。当前无须安装依赖、构建或本地服务器；不要为简单原型引入框架和额外工程。
- 用示例店铺、ASIN 和图片；本地上传、检测、自动推送、版本、权限、结果都应如实说明是否只是模拟。
- 原型中的临时操作不会写回文件，刷新会重置。不要声称推送 GitHub 可以同步浏览器内选图或模拟记录。
- 原始交接文件只保留为背景。如需修正历史资料，优先加顶部说明并在 README 更新结论，不静默改写原始讨论。
- 仓库内文档使用相对路径；对用户交付本机文件时提供当前电脑真实绝对路径链接。不要固化另一台电脑的盘符或用户名。

## 跨电脑同步与发布

- 使用已有远端 `https://github.com/xuqiang97/amazon-asin-image-hub.git`，日常基线为 `main`；执行前以实际远端、分支和配置为准。
- 开始前拉取最新进度，结束时把已确认结论与代码一并提交推送。避免两台电脑同时修改后相互覆盖。
- 工作区有改动时先检查，不自动丢弃、强制重置或强推。存在远端分叉时保留双方工作并解决差异。
- 当前公开仓库的 GitHub Pages 从 `main` 根目录部署。推送前检查即将公开的文件，不提交密钥、凭据、真实业务数据或本机缓存。
- 本地完成不等于已同步；提交不等于已推送；推送不等于 Pages 已更新。交付时分别说明实际完成状态。
- 发布授权成立后，检查完整变更与暂存差异，执行 `git diff --cached --check`，提交并推送；确认本地与远端提交一致、Pages 构建成功、线上文件与仓库内容一致，实际打开线上页面检查关键操作。
- 检查应与变更相称。纯文档不新增测试工程；原型发布按 README 的轻量检查验证，工具生成的截图／临时脚本无需纳入仓库。

## 结束工作时

更新 README 中实际受影响的结论、待确认项、当前进度和下一步，避免另写一份竞争的“最新总结”。回答简洁说明改了什么、验证到哪一步、是否已推送，并给仓库／预览链接。新电脑应能只靠仓库文件接续项目。
