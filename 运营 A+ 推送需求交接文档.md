# 运营 A+ 推送需求——完整上下文

> 历史参考说明（2026-09-14）：本文件保留原 A+ 专题讨论。最新范围以 [README.md](README.md) 为准：A+ 已纳入亚马逊 ASIN 图片中心的独立 Tab；首版仅做增量，本文中的历史整改、批次、阈值和日期边界暂不实施；新增本地上传后的准入与查看权限仍需确认。接口描述属于交接约定，尚未完成真实联调验证。

我正在规划 AI 生图系统里的一个新功能，暂定菜单名为：

> **运营 A+ 推送**

这是一个相对完整的新功能，不是小需求。下面是前期已经讨论并基本达成一致的背景、业务模型、权限、自动化、历史整改和推送链路。请基于这些既有结论继续往下讨论，不要重新从零设计。

---

# 一、需求背景

目前运营 AI 生图支持生成：

- 场景图
- 特写图
- 卖点图
- 标普 A+ 图

其中运营的标普 A+ 图审核通过后，会推送给下游 **A+ 管理系统**，再由 A+ 管理系统上传到 Amazon。

现有链路大致是：

AI 生图任务\
→ 运营审核图片\
→ 审核通过图片推送 A+ 管理\
→ A+ 管理上传 Amazon

现在出现一个核心问题：

## 单次 AI 生图任务很难一次就生成完整可用的一套 A+ 图

例如：

第一次任务生成 7 张：

- 3 张审核通过

第二次任务又生成 7 张：

- 4 张审核通过

业务真正想要的是：

> 第一次的 3 张 + 第二次的 4 张 = 最终完整 7 张 A+ 图

但当前 AI 生图每次都会把当次审核通过图片推送给 A+ 管理。

A+ 管理目前是：

> **以最新一次推送内容覆盖前一次**

因此：

第一次推 3 张\
→ 第二次推 4 张\
→ 最终只剩第二次 4 张

无法累计。

结果就是运营需要去 Amazon 后台人工重新上传和拼图，非常麻烦。

---

# 二、核心产品判断

这次需求不能继续把：

> **一次 AI 生图任务**

等价为：

> **一次完整 A+ 发布**

应该解耦成：

### AI 生图任务

负责：

> 生产素材 → 审核素材

### 运营 A+ 推送任务

负责：

> 从一个或多个历史 AI 生图任务中选择最终图片\
> → 确定顺序\
> → 固化成完整发布快照\
> → 推送 A+ 管理\
> → 查询最终 Amazon 上传结果

因此：

> **生图任务 = 素材生产单位**

> **运营 A+ 推送任务 = A+ 发布单位**

---

# 三、聚合维度

我司的“店铺”本身已经包含站点信息。

例如：

> Amazon-Z01717-US

后缀已经包含 US。

因此本需求不需要额外增加站点维度。

核心商品维度确定为：

> **店铺 + ASIN**

例如：

> Amazon-Z01717-US + B0XXXXXXX

在人工选图时，通过这个维度查询该商品历史 AI 生图素材。

---

# 四、数据权限现状

我司基础权限建设比较差，目前没有：

> 用户 → 店铺 → ASIN

这样的完整基础数据权限。

甚至没有可靠的“店铺 + ASIN”权限表。

之前 AI 生图系统曾接过一个只能查询用户有权限店铺的接口，但：

- 经常漏店铺
- 没人维护
- 业务频繁反馈问题

最后 AI 生图被迫改成：

> 用户可以查询、选择所有店铺

业务侧现实逻辑是：

> 正常运营不会主动花 AI 生图成本，帮别人优化别人负责的 ASIN。

---

# 五、AI 生图现有数据权限

目前 AI 生图的“已完成生图”页面，是按照：

> **生图任务创建人归属**

来控制数据权限。

规则为：

### 普通用户

只能看自己创建的任务。

### 组长

可以看：

> 自己 + 组员

### 管理员

可以看全部。

这是目前 AI 生图已有、可复用的数据权限体系。

---

# 六、运营 A+ 推送权限方案

本需求不应该继续把：

> “任务创建人”

作为最终商品素材的权限边界。

原因包括：

- 销售/运营会频繁离职；
- ASIN 负责人会发生变化；
- 店铺可能封店后通过跟卖转移；
- 亚马逊编辑权可能转移；
- 历史图片本质更接近商品资产，而不是个人资产。

因此目前达成的方案是：

## 第一层：决定用户可以进入哪些店铺 + ASIN

复用现有“已完成生图”的数据权限。

即：

> 当前用户有权限看到的“已完成标普 A+ 生图任务”

按：

> 店铺 + ASIN

去重。

得到该用户可以进入的商品范围。

也就是说：

> **任务权限只负责商品准入。**

---

## 第二层：进入某个店铺 + ASIN 后

用户一旦具备这个商品的准入资格，就可以查询：

> 同店铺 + ASIN\
> + 标普 A+\
> + 已完成任务\
> + 图片审核通过

的全部可用图片。

此时：

> **不再限制任务创建人。**

也就是说：

> 商品准入看任务权限；\
> 商品内部素材可以跨任务、跨创建人复用。

这种“别人也创建过同 ASIN 任务”的情况，实际全量数据里非常罕见。

---

# 七、菜单名称

最开始考虑叫：

> A+ 组稿

但最终更倾向：

> **运营 A+ 推送**

原因是：

“组稿”只是人工多任务选图时的一个中间动作。

这个功能长期实际要承接：

- 单任务自动推送
- 多任务人工聚合推送
- 历史整改重推
- 推送失败处理
- 平台最终结果查询
- 推送历史

所以“运营 A+ 推送”更加准确。

---

# 八、列表的数据粒度

已经确定：

> **列表一行 = 一条独立的 A+ 推送任务 / 推送单**

而不是：

> 一行 = 店铺 + ASIN

同一个店铺 + ASIN 可以存在很多条独立推送记录。

例如：

| 推送任务 | 店铺 | ASIN | 来源 | 状态 |
|---|---|---|---|---|
| PUSH001 | Amazon-Z01717-US | ASIN001 | 自动推送 | 上传成功 |
| PUSH002 | Amazon-Z01717-US | ASIN001 | 人工新建 | 上传成功 |
| PUSH003 | Amazon-Z01717-US | ASIN001 | 历史整改 | 待确认 |

因此不需要额外设计：

> V1 / V2 / V3

每一条独立推送任务本身就是一次完整历史快照。

---

# 九、统一推送任务中心

目前倾向把“运营 A+ 推送”做成：

> **统一 A+ 推送任务中心**

而不是专门做：

> 待处理 A+ Tab\
> 推送记录 Tab

原因是“历史存量整改”完成之后，“待处理 A+”可能长期失去价值。

更好的长期模型是：

每条推送任务增加：

### 任务来源

例如：

- 增量自动推送
- 人工新建
- 历史整改

未来也可以增加：

- 专项补推
- 图片规格整改
- 其他合规整改

再配合：

### 任务状态

例如：

- 待确认
- 待推送
- 处理中
- 上传成功
- 上传失败
- 异常
- 已跳过 / 无需处理

这样历史整改只是统一任务中心中的：

> 一种来源

而不是一套独立长期页面。

---

# 十、批次概念

历史整改可以有：

> batchId / 整改批次

例如：

> 2026 AI 人物 XMP 历史 A+ 整改

但批次不是一级菜单。

它只是推送任务的一个属性。

页面可以：

> 按整改批次筛选

历史整改期间，也可以临时在顶部展示：

> XMP 历史整改：待确认 xxx / 处理中 xxx / 已完成 xxx

点击后自动带入该 batchId 筛选。

整改完成后 Banner 可以隐藏。

---

# 十一、增量自动化必须保留

这是非常重要的原则。

当前已有场景：

> 单个标普 A+ 生图任务一次性 7 张全部审核通过

此时现有系统会：

> 自动推送 A+ 管理

新功能上线后不能让这个场景变得更麻烦。

不能改成：

审核结束\
→ 用户再去“运营 A+ 推送”\
→ 新建任务\
→ 再确认\
→ 再手工推

这是产品倒退。

---

# 十二、增量自动推送策略

以后建议统一走新“运营 A+ 推送”链路。

例如：

某单个生图任务本身已经满足完整发布条件：

> 7 张全部审核通过

系统自动：

1. 创建一条运营 A+ 推送任务；
2. 固化该任务的完整图片快照；
3. 自动调用下游接口；
4. 自动轮询 Amazon 最终上传结果；
5. 最终结果沉淀在统一任务中心。

业务体验仍然：

> 无需额外操作。

---

如果单个生图任务不完整：

例如：

任务 A：

> 3 张审核通过

则：

> 不直接推送

这些审核通过图片成为该：

> 店铺 + ASIN

的可用历史素材。

之后业务再次抽卡：

任务 B：

> 4 张审核通过

业务可以人工新建 A+ 推送：

> 聚合 A + B 的图片\
> → 最终选 7 张\
> → 排序\
> → 推送

---

# 十三、自动推送的重要边界

自动化仅适用于：

> **单任务自身已经构成完整发布套图**

不能因为：

任务 A 3 张\
任务 B 4 张

累计已经 7 张，就让系统自动从两个任务里凑 7 张直接发布。

原因包括：

- 图片可能重复
- 图片质量有高低
- 卖点可能重复
- 最终顺序需要业务判断

因此：

> **跨任务聚合默认属于人工决策。**

---

# 十四、人工新建推送

长期人工流程大致是：

新建推送\
→ 选择店铺\
→ 选择 ASIN\
→ 查询该店铺 + ASIN 历史可用素材\
→ 选择最终图片\
→ 拖拽排序\
→ 确认推送\
→ 创建 A+ 推送 taskId\
→ 固化快照\
→ 调下游接口\
→ 查询最终平台结果

一期目前倾向：

> 普通人工新建不一定需要“保存草稿”。

可以在用户真正点击：

> 确认推送

时才创建正式推送任务。

但历史整改的“待确认任务”属于正式业务任务，并不是用户草稿。

---

# 十五、下游 A+ 管理异步接口

下游接口比较特殊。

不是下游生成 taskId。

而是：

> **我们自己生成并传 taskId。**

链路：

## 接口 1

我方：

> 调 A+ 管理接口1

并传：

> 我方自己的 taskId

用于提交 A+ 上传任务。

---

## 接口 2

之后我方继续拿：

> 同一个 taskId

调用：

> A+ 管理接口2

轮询：

> Amazon 最终上传结果。

因此：

> 一条运营 A+ 推送任务\
> = 一个唯一 A+ 推送 taskId\
> = 一次完整的平台上传任务

---

# 十六、生图任务 ID 与 A+ 推送 taskId 必须解耦

以前单任务自动推送时，看起来可以直接使用 AI 生图任务 ID。

但多任务聚合后这种方式已经不成立。

例如：

最终 7 张来自：

- 生图任务 A
- 生图任务 B
- 生图任务 C

无法选择其中某一个生图任务 ID 代表本次发布。

所以必须新增：

> **独立的 A+ 推送 taskId**

例如：

PUSH001

来源可以是：

> PUSH001 ← IMG001 + IMG002 + IMG003

因此：

### AI 生图任务 ID

代表：

> 素材生产任务

### A+ 推送 taskId

代表：

> 一次平台发布任务

两者必须解耦。

---

# 十七、异步执行状态

业务页面不需要暴露大量技术状态。

业务可以重点看到：

- 待确认
- 处理中
- 上传成功
- 上传失败
- 异常

后台技术状态可以更细：

- 待提交
- 接口1请求中
- 接口1成功
- 接口1结果未知
- 接口2轮询中
- 查询不到任务
- 查询超时
- 最终成功
- 最终失败

原则：

> **接口1请求成功 ≠ Amazon 上传成功**

真正“成功”必须以：

> 接口2最终返回的平台上传结果

为准。

---

# 十八、异步接口后续需要向下游确认的 P0 问题

以下目前还没有完全确认：

1. 同一个 taskId 重复调用接口1是否幂等；
2. 接口1成功后多久接口2可以查到；
3. 接口2“任务不存在”到底代表什么；
4. 接口2有哪些明确状态值；
5. 处理中最大持续时间；
6. 失败之后是否允许相同 taskId 重提。

这些会影响最终重试和补偿机制。

---

# 十九、为什么还需要处理历史存量

除了未来增量之外，本功能还有一个非常重要的上线目的：

> **历史存量 A+ 重推整改。**

背景：

Amazon 有新的 AI 人物图片要求：

> AI 生成人物图片需要携带 XMP 元数据标记。

目前 AI 生图系统已经完成：

> 历史 AI 图片全部检查\
> + 是否含人物标记\
> + XMP 元数据处理

现在需要把相关历史 A+ 图重新上传 Amazon。

因此正好借助：

> 运营 A+ 推送

完成这次历史存量重推。

---

# 二十、历史 / 增量时间边界

历史存量统一以：

> **2026-08-13**

为边界。

原因：

> 2026-08-12 晚上已经彻底闭环后续增量链路。

因此：

> AI 生图任务创建时间 `< 2026-08-13 00:00:00`

视为：

> 历史存量候选。

2026-08-13 及以后：

> 按新的增量闭环流程处理。

---

# 二十一、历史整改基础筛选

历史候选基础条件目前确定为：

> 任务创建时间 < 2026-08-13\
> + 运营标普 A+\
> + 图片状态 = 审核通过

最终按：

> 店铺 + ASIN

归并。

注意：

> 时间边界来源于 AI 生图任务创建时间；

但真正初始化整改任务时：

> **一条整改任务 = 一个店铺 + ASIN**

不是：

> 一个生图任务 = 一个整改任务。

---

# 二十二、无人物图片过滤

由于本次历史整改的核心原因是：

> AI 人物图片 XMP 新规

而系统已经有现成：

> 是否含人物

检测结果。

因此可以进一步减少业务需要确认的数据量。

当前倾向：

对于同一个：

> 店铺 + ASIN

如果其历史符合条件的：

> **审核通过 AI 图片全部不含人物**

则默认：

> 不纳入本次 XMP 历史整改。

人物判断也建议仅针对：

> 审核通过图片

而不是审核不通过图片。

---

# 二十三、历史默认候选素材范围——重点

历史整改初始化时，为了降低误推风险：

> **默认不要直接把这个 ASIN 下所有人的图片全部自动聚合进待发布候选。**

当前认可方案：

## 默认候选集

只聚合：

> **默认责任人自己创建的历史任务中的审核通过图片**

这样更加保守。

但如果用户点击：

> 去选图 / 确认图片

进入详情页后：

> 可以看到该店铺 + ASIN 下所有历史任务的全部审核通过图片

包括：

> 其他人创建的任务。

也就是说：

> **系统自动决策时保守；用户主动决策时开放。**

这是为了避免一些运营很懒：

> 看都不看直接确认推送

导致把别人的历史图片错误地一起推到 Amazon。

---

# 二十四、历史整改默认责任人

一个店铺 + ASIN 只初始化：

> **一条历史整改任务**

不能因为历史上有两个人操作，就给两个人各建一条，否则可能重复推送。

目前倾向的轻量规则：

> 取 2026-08-13 之前，该店铺 + ASIN **最新一条符合条件的生图任务创建人**

作为：

> 默认责任人。

然后：

> 默认候选图片只从这个默认责任人自己的历史任务中取。

如果历史发现：

> 同一个店铺 + ASIN 有多个任务创建人

则属于非常少见的复杂场景。

建议：

> 禁止直接进入完全自动推送\
> → 降级为“待确认”。

---

# 二十五、历史图片初始化顺序

这个规则目前也已经基本确定。

## 单任务

如果候选图片全部来自一个任务：

> **沿用该生图任务原有默认图片顺序。**

---

## 多任务

如果默认责任人历史上有多个任务：

先：

> 按任务创建时间倒序

即：

> 最新任务优先

然后：

> 每个任务内部仍保持该任务自己的默认图片顺序。

例如：

任务 C：08-10

> C1 C2 C3

任务 B：08-05

> B1 B2

任务 A：07-30

> A1 A2

初始化顺序：

> C1 → C2 → C3 → B1 → B2 → A1 → A2

用户进入详情之后：

> 允许自行拖拽调整最终发布顺序。

---

# 二十六、素材池顺序与最终发布顺序要区分

建议：

## 素材池

按：

> 任务创建时间倒序

展示。

方便用户优先查看最近生成图片。

## 最终发布区域

单独维护：

> 第 1 张\
> 第 2 张\
> ……\
> 第 N 张

最终真正传给下游 A+ 管理的：

> 是这个已选发布区的图片顺序。

用户可以：

> 拖拽排序。

---

# 二十七、历史整改自动策略

业务希望历史整改不能全部人工处理。

可以提供：

> **大于等于多少张可用图片时自动推送**

例如：

> N = 7

但是目前讨论认为：

> 不应该仅仅因为“图片数 >= 7”就无脑自动推。

更稳妥的方案是：

业务仍然配置一个简单阈值：

> 自动推送最低候选图片数 N

但系统同时增加一些低成本安全护栏。

例如自动推送需满足：

1. 命中历史整改范围；
2. 标普 A+；
3. 默认候选图片均为审核通过；
4. 默认责任人自己的候选图片数 >= N；
5. 历史任务创建人数 = 1；
6. 图片数据正常；
7. 可以根据既定初始化顺序形成完整发布组合。

满足：

> 自动创建并执行推送任务。

不满足：

> 创建“待确认”整改任务。

---

# 二十八、历史整改任务的典型分类

后续可以形成：

### 自动推送

数据简单、候选明确、安全条件满足。

### 待确认

系统已经预选了一套候选图片，但需要业务确认。

### 待选图

历史多任务、多候选，无法安全自动判断最终组合。

### 异常

例如：

- 图片数据缺失
- 无足够审核通过图片
- 任务数据异常

### 无需处理 / 已跳过

例如：

- ASIN 已停售
- 店铺失效
- 业务已经人工整改
- 当前不需要再推

最好允许业务填写：

> 跳过原因。

---

# 二十九、历史整改与增量最终必须共用同一执行链路

不要为了这次 XMP 历史整改另写一套：

> 历史批量重推脚本

长期增量又走：

> 运营 A+ 推送

正确方向应该是：

历史整改\
→ 创建统一 A+ 推送 taskId\
→ 固化图片快照\
→ 接口1提交\
→ 接口2轮询\
→ 获取最终平台结果

增量自动推送：

> 完全一样。

人工聚合推送：

> 完全一样。

只有：

> task 来源

和：

> 创建前的选图逻辑

不同。

---

# 三十、当前整个功能可以抽象为三个阶段

## 阶段 1：产生最终候选套图

来源可能是：

### 增量单任务自动

单任务已经完整。

### 人工多任务聚合

运营跨任务选图。

### 历史整改初始化

系统按规则预选 / 业务人工确认。

---

## 阶段 2：生成统一推送任务

统一创建：

> A+ 推送 taskId

固化：

- 店铺
- ASIN
- 图片列表
- 图片顺序
- 来源生图任务
- 推送来源
- 创建人
- 整改批次
- 图片快照

---

## 阶段 3：统一执行

> 接口1提交\
> → 接口2轮询\
> → Amazon 最终成功 / 失败

---

# 三十一、当前长期产品结构

菜单：

> **运营 A+ 推送**

核心页面：

> **统一推送任务中心**

列表：

> 一行 = 一条独立 A+ 推送任务

主要能力：

- 新建人工推送
- 查看自动推送记录
- 查看历史整改任务
- 待确认整改
- 推送处理中
- 上传成功 / 失败
- 查看图片快照
- 查看来源生图任务
- 查看最终平台结果
- 条件筛选
- 批次筛选

历史 XMP 整改只是：

> 其中一个任务来源 + 整改批次

而不是永久独立 Tab。

---

# 三十二、设计原则

后续继续设计时，请始终遵循以下原则：

### 1. 不破坏现有增量自动化

单任务一把过必须继续自动推送，不能增加人工操作。

### 2. 自动决策保守，人工操作开放

系统默认不要跨人乱选历史图片。

但用户主动进入详情后，可以查看同店铺 + ASIN 全部历史可用素材。

### 3. 一次推送必须形成不可变快照

一旦提交下游，不应再直接修改原图片组合。

需要换图时：

> 新建新的推送任务。

### 4. 生图任务与发布任务彻底解耦

不要继续复用生图任务 ID 代表平台发布。

### 5. 历史专项不要污染长期 IA

历史 XMP 整改结束后，系统仍然应该长期有价值。

### 6. 页面尽量轻

公司基础建设和研发资源有限，不要为了理论完美设计复杂权限中心、复杂审批流、复杂状态机。

优先：

> 可落地、可解释、风险可控。

---

# 三十三、接下来建议继续讨论的重点

前面的业务模型已经基本成型，下一步不要重新讨论“要不要做聚合”“是不是店铺+ASIN”等已经确定的问题。

建议继续围绕以下内容往下落地：

1. **运营 A+ 推送任务中心列表字段与筛选条件**
2. **人工新建推送的具体交互**
3. **选图详情页的页面结构**
4. **历史整改“待确认”任务的具体交互**
5. **自动推送阈值与配置页面怎么做得足够轻**
6. **图片选择数量、目标数量与校验规则**
7. **拖拽排序和任务内默认顺序**
8. **推送成功 / 失败 / 重试具体规则**
9. **异步接口1、接口2的补偿机制**
10. **历史整改批量确认是否需要做**
11. **如何避免同店铺 + ASIN 并发重复推送**
12. **最终 PRD 与 HTML 原型**

---

# 当前一句话总结

这次需求最终不是简单增加一个“多任务组稿”页面，而是要建设一个：

> **以“店铺 + ASIN”为素材聚合维度、以“一次独立 A+ 推送任务”为发布单位，同时兼容增量自动推送、人工跨任务组稿以及历史 XMP 整改重推，并最终跟踪 Amazon 上传结果的统一「运营 A+ 推送中心」。**

请以上述结论为既有上下文，继续帮助我把功能一步步落到页面、交互、状态、配置、接口规则和 PRD。
